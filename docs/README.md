# Fig Local Revival GitHub Pages

This directory contains the landing page for Fig Local Revival.

## Local Development

To preview locally:

```bash
cd docs
python3 -m http.server 8000
```

Then visit: http://localhost:8000

## GitHub Pages

This site is automatically deployed via GitHub Pages from the `docs/` directory.
